name: ${rootProject.name}
version: ${rootProject.version}
api-version: 1.17
main: net.projecttl.move.server.MoveServerPlugin

commands:
  mserver:
    description: move-server's main command
    usage: /mserver or /mserver <options>
